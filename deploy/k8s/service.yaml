apiVersion: v1
kind: Service
metadata:
  name: echodoc-service
  labels:
    app: echodoc-voice-agent
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: echodoc-voice-agent
